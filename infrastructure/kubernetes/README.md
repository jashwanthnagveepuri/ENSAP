# infrastructure/kubernetes

Placeholder. Phase 9 adds raw Kubernetes manifests (Deployments,
Services, ConfigMaps, Secret references, HPA, PDB, NetworkPolicies) and
validates them on `kind`/`minikube` before any EKS work — see
`docs/14-deployment.md` and master spec §26. Nothing here yet.
