# ticketing-worker

Placeholder (Phase 4). Consumes deployment completion/failure events and
calls `mocks/ticketing-provider` to open/update a ticket — same worker
pattern as `router-worker`, see `docs/06-component-design.md`.
